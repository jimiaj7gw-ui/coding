#include <iostream>
using namespace std;
int main() {
    int x, y, z;
    
    // Assigning all variables the same number
    x = y = z = 50;
    
    // Printing the sum
    cout << x + y + z << endl;
    
    return 0;
}