#include <stdio.h>
//#include <stdlib.h>


int main() {
  int x, y, z;
  //declaring several vars of the same type all at once.
  x = y = z = 50;
  //assigning all the variables the same number.
  printf("%d", x + y + z);
  return 0;
}



